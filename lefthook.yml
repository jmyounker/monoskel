pre-commit:
  parallel: true
  commands:
    check-script-permissions:
      run: bin/check-script-permissions {staged_files}
    check-exe-have-no-ext:
      run: bin/check-exe-have-no-ext {staged_files}
    lint-shell:
      run: bin/lint-shell {staged_files}
